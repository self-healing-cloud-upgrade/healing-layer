# FastAPI and Server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Redis
redis==5.0.1

# Validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Logging
structlog==23.2.0

# HTTP (for traffic generator)
httpx==0.25.2

# Database
sqlalchemy==2.0.23
# psycopg2-binary==2.9.9

docker>=7.0.0

# K3s / Kubernetes API client
# Used by K3s healing strategies (k3s_restart, k3s_scale_up, etc.)
# K3s exposes the standard Kubernetes REST API — this client works unchanged.
# Only active when K3S_ENABLED=true. Stubs are used otherwise.
kubernetes>=28.1.0

# RabbitMQ (Stage 1 — log ingestion)
pika==1.3.2

# OpenSearch (permanent storage)
opensearch-py==2.4.2

# Templating (incident report generation)
Jinja2==3.1.3

# Async SMTP (escalation email notifications)
aiosmtplib>=3.0.0

# WebSocket support (pipeline events SSE/WS endpoint)
websockets==12.0
